export interface Vehicle {
  license_plate: string;
  brand: string;
  model: string;
  capacity: number;
  image: string;
  is_active: boolean;
  model_year: number;
  description: string;
  price_per_day: number;
}